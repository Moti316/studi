# control-report.md — דו"ח-בקרה מתגלגל (צוות builder)

> דו"ח-בקרה מתגלגל מ**ראש-הצוות (יונתן, `builder-lead`) → מתווך (אמיר, `mediator`)**.
> מקבץ את תרומות-הסוכנים מ-`activity-log.md` שלהם לכדי תמונת-מצב-צוותית: Outcome,
> Verdict ודגלים. מוזן ל-`aggregate-report.md` של המתווך בסוף כל מחזור.
> מעודכן: 2026-05-31 · בעלים: `builder-lead` (יונתן).

---

## אופן-הרישום

כל רשומה (append-only) מתעדת מחזור-עבודה צוותי, בפורמט:

```
## [תאריך שעה] מחזור — <כותרת>
- **סוכנים שדיווחו:** <slugs>
- **Outcome צוותי:** <שורת-תקציר>
- **Verdicts:** PASS / CONCERNS / FAIL לכל סוכן
- **דגלים:** סטיות / הפרות-red-line / סחף (slug + סוג + ראיה) — או "אין"
- **הסלמות למתווך:** מה הועלה לאמיר + סיבה — או "אין"
- **Follow-ups פתוחים:** <רשימה>
```

---

## רשומות

(אין רשומות עדיין — יתעדכן בסוף כל מחזור-עבודה צוותי)
