# bugs-and-fixes.md — יומן-באגים צוותי (צוות builder)

> יומן-באגים-ותיקונים צוותי. כל סוכן רושם כאן באג שאיתר ואת תיקונו, כדי שהצוות
> ילמד מטעויות חוזרות וכדי שראש-הצוות (יונתן) יזהה דפוסים. קשור ל-`activity-log.md`
> האישי (שדה Bugs/Fixes) ול-[`control-report.md`](control-report.md).
> מעודכן: 2026-05-31 · בעלים: `builder-lead` (יונתן).

---

## אופן-הרישום

כל רשומה (append-only), בפורמט:

```
## [תאריך] <כותרת-קצרה> — <slug>
- **תסמין:** מה נראה / נכשל.
- **שורש:** הסיבה האמיתית.
- **תיקון:** מה נעשה + קבצים.
- **אימות:** טסט/הרצה שמוכיחים את התיקון.
- **מניעה:** איך נמנע מהישנות (לקח → memory.md / red-line).
```

---

## רשומות

(אין רשומות עדיין)
