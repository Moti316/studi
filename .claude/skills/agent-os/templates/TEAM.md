# TEAM.md — צוות `<team-slug>`

> מסמך-העוגן של הצוות ב-{{PROJECT_NAME}}. נקרא אחרי
> [`../PROJECT-CONTEXT.md`](../PROJECT-CONTEXT.md) ו-[`../ORG.md`](../ORG.md), עם הפרוטוקולים הרלוונטיים לצוות.
> מעודכן: `<YYYY-MM-DD>` · בעלים: `<lead>`.

---

## משימת-הצוות

{{TEAM_MISSION}}

<!--
תאר/י בקצרה את ייעוד-הצוות, גבולות-הסמכות שלו, ואת מה שהוא **אינו** עושה.
אם לצוות סמכות חתומה מיוחדת (למשל צו-עצירה / שער-איכות / quorum-gate),
פרט/י אותה כאן עם תנאי-ההפעלה והראיה (ledger/לוג) שבה היא מתועדת.
-->

---

## חברי-הצוות

| שם       | slug     | תפקיד    | מודל      |
| -------- | -------- | -------- | --------- |
| `<name>` | `<slug>` | `<role>` | {{MODEL}} |
| `<name>` | `<slug>` | `<role>` | {{MODEL}} |
| `<name>` | `<slug>` | `<role>` | {{MODEL}} |

<!--
- שורה לכל חבר-צוות. {{MODEL}} = {{OPUS_MODEL}} (תפקידי-הובלה/שיפוט) או {{SONNET_MODEL}} (ביצוע-שגרתי).
- אם הצוות מחולק לזרועות/תת-קבוצות — שכפל/י את הטבלה תחת כותרת-משנה לכל זרוע.
-->

**ראש-הצוות:** `<lead>` ({{MODEL}}) — מרכז את תוצרי-החברים ל-{{STATE_DOC}} / דו"ח-צוות יחיד.

**גודל:** `<N>` סוכנים, ב-tier `<team-slug>` (מתוך {{TIERS}}).

---

## קו-דיווח

```
חברי-הצוות  →  <lead>  →  the orchestrator  →  {{COUNCIL_MEMBER}}
                   ▲
   תת-זרוע (אם קיימת) מסלימה דרך <sub-lead>
```

<!--
התאם/י את הדיאגרמה לקו-הדיווח בפועל. שני דפוסים נפוצים:
1. דרך-המתווך: חברים → <lead> → the orchestrator → {{COUNCIL_MEMBER}}.
2. ערוץ-ישיר (עוקף-מתווך by-design) לצוותי-בקרה/אי-תלות: חברים → <lead> → {{COUNCIL_MEMBER}}.
-->

- כל חבר מתעד-עצמית ב-`<slug>/activity-log.md` ומדווח ל-`<lead>` (תת-זרוע דרך `<sub-lead>`).
- `<lead>` מאחד את הדיווחים ל-דו"ח-צוות יחיד → מועלה דרך {{HANDOFF_LOG}} / {{COMMS_DIR}}.

---

## הערת-הסלמה

- חריגה/חסם/סטייה-מתוכנית → מסלים מיידית ל-`<lead>` (ולא ממתין לסבב-דיווח שגרתי).
- אם נדרשת הכרעה מעבר-לסמכות-הצוות → `<lead>` מעלה ל-the orchestrator → {{COUNCIL_MEMBER}}.
- סמכות חתומה (אם קיימת לצוות, למשל צו-עצירה): {{OVERSIGHT_QUORUM}} → ראיה ב-ledger → `<<BROADCAST>>` → עוצר מסלול עד {{APPROVAL_CONDITION}}.

---

## קישורי-פרוטוקול

- [`../PROJECT-CONTEXT.md`](../PROJECT-CONTEXT.md) — עוגן-הקשר (נקרא ראשון)
- [`../ORG.md`](../ORG.md) — היררכיה · פרוטוקולים · קצב-דיווח · בקרת-סחף · מחזור-חיים
- `<team-slug>/_<protocol>.md` — פרוטוקולים ספציפיים-לצוות (אם קיימים)
- `<slug>/{identity.md, memory.md, activity-log.md}` — קבצי-הסוכן הפר-חברי

---

> כל סוכן בצוות קורא `PROJECT-CONTEXT.md` + `identity.md` + `memory.md` בתחילת-הפעלה,
> ומתעד ב-`activity-log.md` בסיום. ראה/י את הפרוטוקול המלא ב-[`../ORG.md`](../ORG.md).
