# 📨 MOTI-INBOX — תיבת-הערות דו-כיוונית (מוטי ↔ Claude)

> **ערוץ-תקשורת א-סינכרוני.** מוטי כותב כאן הערות/הנחיות מכל מחשב (push ל-`main`);
> Claude קורא את הקובץ ב-**צעד-0 של כל משימה** (אחרי `git pull`), מבצע, ומשיב inline.
> מעודכן: 2026-06-04.

## איך זה עובד

1. **מוטי** מוסיף רשומה תחת "## הערות מ-מוטי" (כותרת + טקסט) ו-push ל-`main`.
2. **Claude** קורא בתחילת כל משימה; לכל הערה פתוחה → מבצע, ומוסיף מתחתיה בלוק `🤖 תשובה:` + מסמן `✅ טופל`.
3. הערות-שטופלו נשארות כתיעוד (לא נמחקות), עם סימון.

**סטטוס:** 🆕 חדש (לא-נקרא) · 🔄 בטיפול · ✅ טופל · ⏸️ ממתין-להבהרה.

---

## הערות מ-מוטי

> (ריק כרגע — הוסף כאן הערות. הריצה האוטונומית של הלילה החלה 2026-06-04; ראה `SESSION-LOG.md`.)

---

## ארכיון-תשובות (שטופלו)

> (Claude מעביר לכאן הערות-שטופלו בסוף סשן, לשמירת-היסטוריה.)
